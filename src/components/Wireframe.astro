---
---
<div class="wireframe">
  <div class="wireframe-header">src/payments/charge.ts</div>
  <div class="wireframe-cols">
    <div class="wireframe-col">
      <div class="wireframe-col-label">whiteboard</div>
      <pre>{`fn charge(order):
  validate order
  call stripe  (no retry)
  record txn
  return result

`}<span class="warn">⚠ no try/catch</span></pre>
    </div>
    <div class="wireframe-col">
      <div class="wireframe-col-label">live diff</div>
      <pre><span class="add">{`+ export async function
+   charge(order) {
+     await stripe.charges
+       .create({ ... })
+     await db.txns.insert(r)
+     return r
+   }`}</span>{`

`}<span class="note">{`▸ matched the spec
▸ open: idempotency key?`}</span></pre>
    </div>
  </div>
  <div class="wireframe-comments">
    <span class="who">navid</span>don't catch errors here. let them bubble.
  </div>
</div>
