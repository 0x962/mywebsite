---
export const prerender = true;
/**
 * /wip/prompt/ — Excalidraw canvas post (empty draft). Metadata:
 * src/content/posts/prompt.mdx; scene read from src/data/scenes/prompt.json
 * by ExcalidrawPost.
 */
import ExcalidrawPost from '../../components/excalidraw/ExcalidrawPost.astro';
---
<ExcalidrawPost slug="prompt" />
